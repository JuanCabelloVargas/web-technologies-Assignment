<div class="container mt-3">  
  <h1 class="mb-3 text-primary">Categories</h1>
    <ul>
      <% @categories.each do |category| %>
        <li>
          <%= link_to category.name, category_path(category) %>
        </li>
      <% end %>
    </ul>
</div>